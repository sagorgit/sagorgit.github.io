on:
  push:
    - workflows: [build-check]
      filter:
        branches: ["main"]

workflows:
  build-check:
    tasks:
      - name: check
        cubes:
          - action: actions/checkout@v4
          - script:
              - echo "Site files are already in the public folder."